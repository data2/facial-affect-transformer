

## 🎯 **使用方法**

### 1. **自动继续训练（推荐）**
```bash
# 自动查找最新检查点并继续训练
python train.py --auto
```

### 2. **从头开始训练**
```bash
# 不指定任何参数，从头开始
python train.py
```

### 3. **从指定检查点继续**
```bash
# 从最佳模型继续
python train.py --resume best_model_80_target.pth

# 从第10轮检查点继续
python train.py --resume checkpoint_epoch_010.pth --epoch 10
```

## 🔧 **主要功能**

1. **disgust权重自动调整**：9.4 → 3.0
2. **自动resume功能**：`--auto`参数自动查找最新检查点
3. **手动resume功能**：`--resume`指定检查点
4. **优化配置**：学习率2e-5，增强强度降低
5. **完整训练记录**：保存最佳模型和检查点

## 📁 **支持的检查点**

```
checkpoint_epoch_005.pth    # 第5轮检查点
checkpoint_epoch_010.pth    # 第10轮检查点
best_model_80_target.pth    # 最佳模型
```

## 🚀 **立即运行**

从您最新的检查点（第10轮）继续训练：

```bash
python train.py --auto
```

或者从最佳模型继续：

```bash
python train.py --resume best_model_80_target.pth
```

**这样下次训练中断后，运行`python train.py --auto`就会自动从最新位置继续！** 🎉